Option Explicit

Private Sub UserForm_Initialize()
    'If IsMac = False Then
        ' Masquer la barre de titre Windows.
        ' ufProgress.Height = ufProgress.Height - 10
        ' HideTitleBar.HideTitleBar ufProgress
    'End If
End Sub
